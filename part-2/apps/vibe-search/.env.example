# Optional locally, required on Render after the lockdown step.
# Must match part-2/apps/web VIBE_SEARCH_API_KEY.
VIBE_SEARCH_API_KEY=pHZos5x4V7vmaK

# Optional. Defaults to all-MiniLM-L6-v2 if omitted.
MODEL_NAME=all-MiniLM-L6-v2

# Sentry, use the DSN from the vibe-search-api project.
SENTRY_DSN=https://your-fastapi-dsn@o000000.ingest.sentry.io/0000000
SENTRY_ENVIRONMENT=development

# Instructor fallback only. Keep false unless deliberately testing /api/debug-sentry.
SENTRY_DEBUG_ENABLED=false
